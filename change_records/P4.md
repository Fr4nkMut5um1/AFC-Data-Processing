# P4 实际 S3 绘图搬迁

变更：两个主脚本的实际 S3 绘制体搬到本地 `plot_section3_preview(results,cfg,phase_contour)`。函数实际计算原绘制剖面/色限并画图，不转发回旧主脚本。`plot_phase_averaged_maps` 及其样式辅助代码原样保留。主脚本保留受控 case 原有 PostProc 条件与 `phase_stats_cache` 调用。

不变：移动体逐字符比较一致，仅 baseline 的地图参数从 `results.phase` 变为调用处明确赋值的 `phase_contour=results.phase`。smooth_points 已在 P3 以原值 5 暴露。数据、归一化、坐标/limits/CLim/ocean256、marker、字号、位置、图名均保留。预览仍不导出文件。错误清单由函数返回，失败时不打印 5 张完整成功。

源码澄清：原 controlled 主脚本（P2 版本）L593 注释称“不计算三重分解”，下一调用实际为完整 phase_stats_cache；更正注释不改调用。原相位云图来自 phase_contour，但 CLim 仍来自 results.phase，这一既存口径不在本批修复。

S2 继续使用现有实际绘图任务函数 plot_core_products 内的 render_*；本批没有需要改动的 S2 样式逻辑，不继续为拆分而拆分。S4/S5 专用绘图不动。

验证：P4_STATIC.json 记录两移动体指纹，归一后相同；静态语法通过。无 MATLAB，未以相同 MAT 重画、未核对图形对象数据/样式；动态待验收。
