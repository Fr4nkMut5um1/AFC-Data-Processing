# P3 参数集中（静态批次）
先建立 P3_FIELD_MAP.csv：以 P2 实际主脚本/消费者为依据，历史配置不作参数真值。

- 删除两脚本 S4 顶层重复入口，仅保留 structures.section4；旧库兼容默认分支仍保留。
- stages.cache 唯一重建入口，rebuild_cache 在 Section 0 派生一次；Section 1/缓存函数不再重复覆盖。
- 移除无消费者的 profile_source 和 S4 plot_colormap 用户入口，实际来源/色图不改变。
- 显示暴露 S5 stress_fraction_floor=1e-6、S3 显示平滑 5 点、S4 参考区间/幅值/分块原值。
- 日常 S5 继续 cfg→section5_run；旧配置入口仅 maintenance/run_section5_legacy_config，显式指定文件，不猜旧配置或迁移路径。

S5 source 选择整个分析源；frame_mode 约束全部矩；edge 限制梯度/生产有效域，profile 范围只控制剖面平均；H 仍由 friction.hole_thresholds 定义一次并严格大于；fraction_floor 只保护应力份额分母。概率分母、同源支持域、repeat 分解与合并不变。

验证：参数原值/赋值位置和修改 diff 静态检查；MISS_HIT 语法检查见后续总记录。未运行 MATLAB，不代表数值或编辑器 Run Section 验收。独立 d23 核心、S2/3 科学算法及 S5 核心无修改。
